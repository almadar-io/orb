/**
 * Orbital Types for HeroSchemaAnimation
 * Re-exporting from @almadar/core to ensure strict type compliance.
 */

export type {
    OrbitalSchema,
    Orbital,
    Trait,
    TraitRef,
    State,
    StateMachine,
    Event,
    Transition,
    Page,
    PageRef,
    Entity,
    EntityRef
} from '@almadar/core';
